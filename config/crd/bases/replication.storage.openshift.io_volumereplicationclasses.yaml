
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.4.1
  creationTimestamp: null
  name: volumereplicationclasses.replication.storage.openshift.io
spec:
  group: replication.storage.openshift.io
  names:
    kind: VolumeReplicationClass
    listKind: VolumeReplicationClassList
    plural: volumereplicationclasses
    singular: volumereplicationclass
  scope: Cluster
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: VolumeReplicationClass specifies parameters that a underlying
          storage system uses when creating a volume replica. A specific VolumeReplicationClass
          is used by specifying its name in a VolumeReplicationRequest object. VolumeReplicationClasses
          are non-namespaced
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: VolumeReplicationClassSpec specifies parameters that a underlying
              storage system uses when creating a volume replica. A specific VolumeReplicationClass
              is used by specifying its name in a VolumeReplicationRequest object.
            properties:
              driver:
                description: driver is the name of the storage driver that handles
                  this VolumeReplicationClass. Required
                type: string
              parameters:
                additionalProperties:
                  type: string
                description: parameters is a key-value map with storage driver specific
                  parameters for creating the volume replicas. These values are opaque
                  to Kubernetes.
                type: object
            required:
            - driver
            type: object
          status:
            description: VolumeReplicationClassStatus defines the observed state of
              VolumeReplicationClass
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: []
  storedVersions: []
